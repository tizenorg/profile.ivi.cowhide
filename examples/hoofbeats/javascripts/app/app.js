/* vi: set et sw=4 ts=4 si: */
(function(win) {
    'use strict';

    win.Hoofbeats = window.Ember.Application.create({
        VERSION: '0.1',
        rootElement: '#application'
    });
})(window);
